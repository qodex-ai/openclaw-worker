# AWS Credentials for Terraform & AWS CLI
#
# Setup Instructions:
# 1. Copy this file:     cp .aws.env.example .aws.env
# 2. Fill in your AWS credentials from IAM user
# 3. Load credentials:   source .aws.env
# 4. Verify:             aws sts get-caller-identity
#
# SECURITY: This file is git-ignored. Never commit credentials to git!
# ============================================================================

# Your IAM user access key (starts with AKIA...)
AWS_ACCESS_KEY_ID=AKIA________________

# Your IAM user secret access key
AWS_SECRET_ACCESS_KEY=________________________________________

# AWS region where resources will be created
AWS_DEFAULT_REGION=us-east-1

# Optional: Set output format for AWS CLI
AWS_DEFAULT_OUTPUT=json

# ============================================================================
# Usage:
#   source .aws.env          # Load credentials
#   terraform init           # Initialize Terraform
#   terraform apply          # Deploy infrastructure
#   aws s3 ls                # Use AWS CLI commands
# ============================================================================
